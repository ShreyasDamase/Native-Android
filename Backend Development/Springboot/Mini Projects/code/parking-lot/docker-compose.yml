services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: parking_lot
      POSTGRES_USER: parking
      POSTGRES_PASSWORD: parking
    ports:
      - "5433:5432"
    volumes:
      - parking_lot_data:/var/lib/postgresql/data

volumes:
  parking_lot_data:
