services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ticket_booking
      POSTGRES_USER: booking
      POSTGRES_PASSWORD: booking
    ports:
      - "5434:5432"
    volumes:
      - ticket_booking_data:/var/lib/postgresql/data

volumes:
  ticket_booking_data:
