services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: wallet_ledger
      POSTGRES_USER: wallet
      POSTGRES_PASSWORD: wallet
    ports:
      - "5436:5432"
    volumes:
      - wallet_ledger_data:/var/lib/postgresql/data

volumes:
  wallet_ledger_data:
